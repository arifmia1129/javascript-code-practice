/* // Basic Javascript App
const instituteName = prompt("What is your institute name?");
const roll = prompt("What is your roll number?");
const semesterFee = Number(prompt("How much your semester fee?"));
const discountAmount = Number(prompt("Discount %?")) / 100;
const payableAmount = semesterFee - discountAmount;

alert(`Institute: ${instituteName}\nRoll:${roll}\nSemester Fee: ${semesterFee}\nDiscount: ${discountAmount}\nPayable Amount: ${payableAmount}`); */